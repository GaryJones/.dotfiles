# Reset the PATH to sane defaults
export PATH="$HOME/.brew/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"

# Load antigen
source "$HOME/.antigen/antigen.zsh"
antigen use oh-my-zsh
antigen bundle git
antigen theme candy
antigen apply
